@page
@model MusicForge.Web.Pages.CreateArticle
@{
}
<div class="loginContainer">
<h3>Create Article</h3>
@if (Model.ErrorMessage != null)
{
    <p style="color:red">@Model.ErrorMessage</p>
}
<hr>

<form method="post" enctype="multipart/form-data">
    <div>
        <label for="Title">Title</label><br />
        <input type="text" id="Title" name="Title" asp-for="Title" required />
    </div>
    <br />
    <div>
        <label for="UploadedFile">Text File</label><br />
        <input type="file" id="UploadedFile" name="UploadedFile" accept=".txt,.csv,.log,.md" />
    </div>
    <br />
    <button type="submit">Upload</button>
</form>
</div>

@if (Model.FileContents != null)
{
    <h3>Result</h3>
    <p><strong>Title:</strong> @Model.Title</p>
    <pre>@Model.FileContents</pre>
}
